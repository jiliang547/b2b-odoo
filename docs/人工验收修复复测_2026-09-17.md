# 人工验收修复与复测（进行中）

> 当前不是“全量验收通过”报告。原始 147 条用例的 41 通过、11 失败、54 部分完成、19 阻塞、22 未执行是修复前基线，不应直接当成修复后的统计。本文件按实际补测场景提供增量证据，尚未逐条重算全量矩阵。

本文件补充同日原始验收报告，不覆盖原始失败证据。代码修复、自动回归与浏览器人工操作结果分开记录；未执行项目不算通过。

## 环境

- 本地 8070 / MC UAT Portal；只使用测试账号、Demo 支付与模拟银行凭证。
- 2026-09-17 升级 b2b_website、b2b_multicompany 后恢复 PartnerHub-Local-8070 服务。
- 首次自动测试筛选未匹配用例；第二次在现有库的原生 ProductCommon 初始化时被已配置价格表保护拦截，均不算通过。随后新建独立 b2b_uat_fixes_20260917 数据库验证，不关闭业务校验。

## 已完成浏览器复测

| 问题 | 实际操作与结果 | 状态 |
| --- | --- | --- |
| BUG01 重复开放改单 | E 客户对 S01754，两标签同时点击 Submit Request：第一条 OCR/MC/2026/00020 Submitted；第二页显示 Another change request is already open for this order，并隐藏再次提交入口。 | 此复现场景通过 |
| BUG07 校验后表单消失 | S01754 输入三个空格、备注后提交；显示至少 10 字符提示，表单、提交按钮和原备注均保留。 | 通过 |
| BUG08 拒绝后历史方案隐藏 | E 客户打开 OCR00010（Cancelled），仍显示提案产品 100、运费 50、修订金额 150、差额 0。未发出提案即拒绝的 OCR00008 仍显示 Pending review，不伪造报价。 | 已发提案历史通过 |
| BUG10 已取消订单错误入口 | S01759 collection：无当前 PI 下载按钮、无上传/支付按钮、无外部履约提示；提示取消不等于退款，已有 200 需要财务处理。 | 通过 |

其他缺陷和未覆盖分支仍在验证，不据此宣称全部验收完成。

## 后续复测记录

- BUG05：E 客户真实购物车下单 S01770（1769），1000 USD，银行付款。填写 400、2026-09-17、UAT-FIX05-BROKEN-PDF 和备注，选择 uat-header-only-proof.pdf 并提交：文件被拒绝、无凭证记录，金额/日期/参考号/备注保留。改选桌面正常 PNG 后成功生成 BTR00048，待审核金额 400，已收仍 0。损坏拒绝与正常上传均通过。
- 自动检查：scripts/test_payment_form.mjs 的 9 项通过；独立空库安装 Demo 支付后，TestB2BCollection 41 项测试全部通过（0 failed / 0 errors）。此前缺少 Demo 模块的 3 项初始化依赖错误不计通过，以上是补齐后重新运行结果。
- 自动检查补充：TestB2BOrderChange 16 项通过，0 failed / 0 errors。
- BUG09：BTR00048 关联原生 E 公司付款 PBNK1/2026/00015（405），确认 400 后客户显示已收 400、剩余 600。原生 Reset to Draft 后客户已收回到 0，历史行显示 `Receipt reversed or unposted — not counted`、计入金额 0。随后原生 Confirm 恢复同一付款，未创建第二笔。通过。
- BUG03：S01770 部分银行到账 400 后，点击实际 `Pay Online` 链接（payment_amount=600）正常打开 Pay Order，明确显示本次 600、总报价 1000，不再 404。此分支通过。
- BUG04（支付中改价）：S01770 发起 Demo Pending 600，运营把数量 9 改为 8 并点击 Save manually，服务端拒绝，提示先等待或取消支付。已放弃未保存修改，并通过 Demo 原生 Cancel 取消该笔测试交易。此分支通过。
- 补充自动回归：新增数据库部分唯一索引存在性检查、native cron 修复已标记 post-processed 但改单仍 balance_due 的测试，18 项改单测试全部通过。
- 权限观察：当前 MC UAT Operations 在原生 Demo 交易点击 Post-process 被原生 payment.provider 管理员 ACL 阻止；没有扩大该用户权限，不能将这个管理员入口算作运营可操作。客户侧取消反馈正常。
- BUG04（陈旧报价）：确认取消 Pending 后，后台保存并重新打开核实 S01770 总额从 1000 降到 900，客户旧窗口 600 被拒绝；反向保留 900 报价/500 待付窗口，后台保存回 1000 后旧 500 同样被拒绝。两次均提示 `Your order has changed. Refresh the page and review the latest quotation before paying.`，没有静默按旧金额收费。通过。
- 源码检查脚本发现既有 `setup.xml` 使用 template 后又 record 更新同一 external_collection_notice XMLID；这是 Odoo 合法的记录更新，脚本误判为重复定义。本轮模块实际升级成功；未为了让脚本变绿删除这条升级兼容记录。
- S01770 最新报价正常 Demo 600 付款成功，银行 400 + Demo 600 = 已收 1000，Order Confirmed / Fully Paid。原生 PDF 生成导致约一分钟等待，最终自动返回订单，无需再次付款；作为性能/交互待优化项保留。
- BUG06 自动回归补充：由非待办原负责人完成财务流程，自动待办按照 Odoo 19 原生行为归档（不是删除），无关人工待办仍 active。最初测试错误地断言记录删除，改为验证原生 active 后通过；未更改原生归档逻辑。
- BUG02 新补款闭环：S01770 已收 1000，客户发起 OCR00021；运营 Start Review → Edit Proposed Order，数量 9→10，Save & Close；填写模拟工厂许可并确认 → Send to Customer；客户 Accept Revised Order，差额 100。此次复测发现初版 BUG03 修复把已确认订单的付款金额参数也清空，导致 Pay Online 不开付款框；已修正为只绕过未确认报价的原始定金检查，已确认订单按实时应付金额打开原生窗口。重启后窗口显示 100，Demo 支付成功，订单总额/已收 1100、余额 0，后台 OCR00021 Completed，客户重新出现 Request an Order Change。新补款闭环通过；旧 S01746 的历史恢复仍需执行原生恢复任务核验，不混为已修复数据。

## 最新补测结果

- BUG02 历史恢复已完成：仅对本地测试库执行 Odoo 原生支付后处理恢复任务。S01746 / OCR00006 从 Balance Due 恢复 Completed；恢复前后实收均为 360，没有新增收款。客户页与运营页分别核对通过。此项是“原生任务修复 + UI 核验”，不冒充重新人工付款。
- 最新自动回归：`uat-prg-final-20260917.log`，60 项后端测试，0 failed / 0 errors；包含上传成功 303 跳转和一次性成功消息回归。JavaScript 9 项通过。自动测试不是替代人工验收。
- BUG11 新快照：S01770 点击 Download Current PI，签发 PI40/v1；渲染 PDF 为图片人工查看，Beneficiary Address 为 Hong Kong，无空逗号；订单总额/实收 1100、余额 0，与前端一致。再次实际点击下载，仍 PI40/v1，没有重复版本。
- BUG11 历史限制：S01746 的旧银行快照仍为 `, , , Hong Kong`，新签发 v1/v2 仍沿用旧快照。现有修复只修正新快照生成，没有批量改写既有快照或已签发 PDF。因此旧数据场景尚未完全修复，不能标记 BUG11 全部关闭。
- 跨客户附件权限：E 登录访问 B 订单 S01746 的 PI，返回品牌化 404；B 登录访问 E 订单 S01770 的凭证 327/8910，返回 404，未下载到附件。
- 新发现并修复：上传成功以前直接返回 POST 页面，刷新可能保留旧响应并触发重新提交。现改为原生 303 跳转正常 collection GET，并用会话一次性成功提醒。浏览器实测 S01746 上传 1 USD 模拟凭证，仅生成 BTR00049；刷新后仍一条记录，无重复成功提示。运营填写明确测试说明并 Reject，客户可见拒绝原因，实收仍 360；没有创建银行付款。
- 定金足够且降额不退款：B 客户 S01746 原总额1200、实收360，申请 OCR00022；运营编辑 S01772 提案数量11→10、运费100不变，保存、记录模拟生产许可并发送；客户接受1100提案后立即 Completed，后台 Actual Refund Required=0。客户付款页实收360、尾款740，无错误的100退款或再次定金要求。此场景通过，不等同于所有比例/金额组合都已覆盖。
- PI 版本：上述改单后实际下载当前 PI，新增 PI41/v2；v1/PI39 保留，金额1200；v2金额1100、实收360、余额740。新旧版本入口均显示，历史 v1 实际点击下载成功。银行快照保持不变（包含上文历史格式问题），未覆盖所有银行切换及品牌logo组合。

## 仍需继续的验收与阻塞

以下不能计入本轮通过：

- 独立纯运营/纯财务角色边界、其他联系人共享订单、跨人员退款待办关闭的完整人工操作。
- 多币种银行到账/退款、税费和折扣组合、B20与E40各比例的完整交叉组合、定金超过修订总额退款、等额换产品。
- 已开票改单、重复关联退款流水、退款撤销、报价过期、MOQ/隐藏产品、未审批账号、信用额度、网络中断、移动端适配等未完成的具体用例。
- 外部 ERP 尚未接入，实际生产/发货状态分支不能验收为通过；本轮“工厂许可”均明确为测试模拟，不代表真实工厂许可。
- 原测试库原生发票过账曾被未信任银行配置阻断；未绕过原生控制。测试邮箱为 example.test，不验证真实邮件送达。
- 当前 MC UAT Operations 混合多种业务权限，不能用该账号证明最小权限角色安全；其不能操作 payment.provider 管理员功能属于原生权限保护，不直接扩大权限。

## 本轮交付边界与建议

- 修复继续沿用原生订单、付款、银行日记账、支付后处理、活动归档、QWeb/PDF，只补上并发、失效状态和前端表单边界控制。
- 建议正式部署前完成上述金融/权限剩余分支；不要将“60项自动测试通过”解释成“147项人工验收全部完成”。
- Demo 支付成功后的原生 PDF 处理曾等待约一分钟，最终正常返回。建议后续单独评估报告生成耗时与等待提示，避免用户重复点击付款。
- 所有改动仅在本地，未推送 GitHub、未部署 Odoo.sh。8070 已恢复。测试资料和审计记录保留，不删除会计凭证。
