# 用友 C4 销售订单对接验收记录

日期：2026-09-23。范围：首次创建销售订单；不包括改单、物流回传和财务记账接口。

## 1. 环境和安全边界

- 本地网站：8072，数据库 `b2b_ai_test_20260918`。
- C4：仅测试管理/使用/销售/开票/库存组织 `999`，没有向正式组织写入。
- 新增 ERP 测试客户 `PH_TEST_ORDER_CN_20260923`，用于验证维护 CNY 币种、中国大陆、13% 税率。另复用已验证的 RO 客户 `PH_TEST_53C1CEAA5A9FE523`，验证币种未维护时默认 USD。
- 测试物料：000020；没有修改 ERP 物料主数据。
- Demo 付款不扣真实款项。原有订单没有补传，没有自动审核 ERP 单据或安排出库。
- 测试单据保留用于核查，ERP 状态为 0（开立）。

## 2. 模拟人工操作及真实 C4 结果

后台使用测试运营账号实际打开报价单、点击 Check ERP Order Setup、Confirm、ERP Jobs / Process Now；客户使用测试门户账号实际打开 Quotations → View Details → Accept & Pay → Demo → Successful → Pay，检查自动跳转和结果。测试资料准备通过本地 ORM 完成，不将“脚本直接改订单状态”作为客户点击测试通过的依据。

| 场景 | Odoo 单号 | ERP 单号 | 最终结果 |
| --- | --- | --- | --- |
| USD、0% 税、多明细、折扣、后台确认 | S00378 | PH-T-2026-000017-f89aaafd | Success；未税/含税均 205 USD，汇率 6.3 |
| ERP 客户维护 CNY、中国大陆13%税 | S00379 | PH-T-2026-000018-43940276 | Success；未税205、税26.65、总额231.65 CNY，汇率1 |
| 客户 Demo 付款；修复后恢复已有成功付款的确认处理 | S00380 | PH-T-2026-000033-236cf2ea | Success；12.50 USD；同一付款 done，没有重复扣款/创建新交易 |
| 修复后新建报价，客户从头点击 Demo 付款 | S00550 | PH-T-2026-000034-ec4e65b1 | Success；15 USD；自动回到 Your payment has been processed，原生无税可用 |

四张订单均查询 ERP 明细复核：客户、销售组织、开票组织、库存组织、部门05、业务员LQS0008、原币/本币、6.3或1汇率、不含税标志、无插头/无电压、打托、明细数量、折后金额、税额、合计、普通销售、计划发货2026-12-22。Odoo 每单只有一个真实 ERP 任务和一个 ERP 单号，任务均为 Success。

ERP IDs：

- S00378：2636620904734916634
- S00379：2636625354321035270
- S00380：2636634390933274644
- S00550：2636635198390272009

补充后台实点：在 B2B Management → Configuration → Yonyou ERP Mapping 看到销售组织999、汇率6.30000000和独立下单开关；取消勾选并点击 Save manually 后持久化成功。在 Customers → 测试客户公司 → Partner Hub 选择并保存 `UAT ERP Export Tax Rule 20260923`，随后通过原生模型核对确实写入客户财政位置，其联系人新报价的产品默认13%被映射成0%，100未税/100含税。核对用临时报价已回滚，没有传ERP。

## 3. 本次发现并修复

| 问题 | 原因与修复 | 验证方式 |
| --- | --- | --- |
| 多明细订单误判成重复单据，已创建但未显示成功 | ERP 列表按订单行返回，同一单号/ID出现多行；改为完整分页、按订单ID去重，只有不同ID才视为冲突 | S00378 查回已有单据成功；重复ID跨页和同号不同ID测试 |
| 13%税订单被ERP严格金额校验拒绝 | ORM 浮点尾差进入JSON，例如23.400000000000006；发送前按币种精度规范金额，保留Odoo商业金额 | S00379成功；精确23.4断言，不重复加税 |
| 客户付款后 ERP 队列拒绝创建 | sudo 回调保留门户uid，旧队列要求内部身份；仅在私有“已确认订单入队”边界使用系统身份 | S00380恢复；S00550从头点击成功；门户回调单测与门户无越权断言 |
| 未保存报价单产生 NewId domain 警告 | 计算ERP状态前先处理未保存记录，已有记录使用原记录ID | 原生后台表单回归 |

测试脚本另修正了运费产品夹具创建方式；本次自己创建的两个 UAT 价格表明确设为共享，避免当前公司默认值污染跨公司样品测试。没有为此修改业务税额或放宽跨公司安全校验。

原有加购浏览器测试还有一处冷加载时序问题：固定等待1.5秒后 `requestSubmit()` 会绕过尚未启用的按钮，触发原生GET表单导航并导致脚本超时。已改为等待按钮可用后 `.click()`，保留购物车数量必须增加和不得出现失败通知的断言；未修改实际加购业务代码。银行触发测试对受限快照的内部断言使用sudo读取，实际财务收款操作仍以原财务测试用户执行，不给财务额外集成权限。

## 4. 自动化覆盖清单

- 确认订单时无外部HTTP、重复确认只有一个任务；固定32位防重键。
- 准备请求与外部发送分开提交；发送前查询已存在单；丢失创建响应后找回原单，不重复创建。
- 客户币种默认USD、维护币种优先、未知币种ID不误作USD、已确认订单币种不被替换。
- 默认业务员/部门必须属于目标使用组织。
- Odoo 原生客户财政位置、公司联系人继承、13%税不重复加算、原生无税等价0%。
- 运费服务SKU作为普通映射行，不额外增加独立运费字段。
- 30%定金：20%时不传，补10%确认时按整单100%排队，余70%付款不重复建单。
- 税、币种、物料或身份不一致时阻止发送；已付款本地订单不被反向修改。
- 原订单改单/取消标记人工核对；门户用户不能伪造ERP状态或请求快照。
- ERP任务按员工允许销售公司隔离；组织999限制不可通过前端上下文绕过。
- 原有核心、ERP队列、网站收款/改单/注册/售后、多公司流程回归。

### 最终回归结果

本轮执行了337项全量回归。末次全量运行335项通过，剩下两项是加购浏览器用例的测试等待条件：Odoo要求ready表达式返回布尔true，而不是DOM元素，已补为显式布尔判断。

随后使用最终代码重跑**全部56项用友测试和2项浏览器加购测试，共58项：0失败、0错误**。两条浏览器用例均记录 `test successful`。因此全量发现的未通过项均已修复并复测通过；这里不把分批验证写成“单次337项全绿”。

日志：

- `output/yonyou-orders-full-regression.log`：最初全量运行及问题定位。
- `output/yonyou-orders-final-regression.log`：337项全量运行记录（保留历史失败，不覆盖掩盖）。
- `output/yonyou-orders-focused-regression.log`：最终58项通过，结束时间2026-09-23 06:41:13 UTC。
- `output/ai-preview.log`：本地实际客户点击付款、回调确认及后台任务处理。

最终测试过程未出现新的应用WARNING；权限拒绝用例会按预期记录INFO级拒绝访问，不是失败。

## 5. 尚不应误认为已验收的内容

- 正式组织、真实 PayPal 扣款、真实银行到账，没有在本次进行。
- 银行定金/尾款触发规则使用原生收款模型自动化验收，本次没有重新全程点击银行凭证上传和财务表单。
- 运费映射机制已自动化验证；真实ERP运费料号尚待业务配置，不声称已用真实运费物料联调。
- 本次不是完整网站从商品列表加购到结算的全量UAT；客户实点覆盖报价详情付款闭环，后台实点覆盖订单检查、确认和任务处理。
- ERP改单、取消、订单追踪、生产/发货/物流回传尚待下一阶段规则；本期不自动调用这些写接口。
- 网站配送地址、客户备注未新增专门ERP字段映射，仍需ERP审核人员人工核对。
- Odoo.sh尚未部署本轮代码；本次不推送GitHub。

## 6. 交付时本地状态

8072保持运行。映射查询和原注册审批配置保留；本次临时启用的 **Send Newly Confirmed Orders to ERP** 已通过界面关闭，避免没有映射的旧测试数据突然被纳入自动传单。需要继续验收时按操作说明启用，仍只能写测试组织999。既有成功任务、客户/物料绑定和税规则不会因关闭开关消失。

全量回归使用数据库副本 `b2b_yonyou_regression_20260923`，不作为第二个客户预览站点开放；测试端口18072随测试进程退出关闭。

## 7. 默认业务员解析兼容性补充验收

客户 `LT2026030311` 的管理组织为 `global00`，在使用组织 `999` 下的明细直接返回 `principals`，不包含 `merchantApplyRanges`。原解析要求先匹配范围表，导致默认业务员和部门被漏取；这不是客户没有维护负责人。

修复兼容两种返回结构：有范围表时仍严格按目标组织匹配；无范围表时，先核验 `merchantAppliedDetail.belongOrgId___code`，再读取该组织明细内唯一默认负责人。明确的客户/组织归属冲突、混合范围或多个默认负责人均不猜测。查回订单时新增业务员和部门一致性核对。

- 全部用友模块回归 **64项，0失败、0错误**，包含本次新增8项测试（部分用例含多个子场景）。日志：`output/yonyou-principal-regression.log`。
- 真实C4查询复测：`LT2026030311` 正确提取 `LQSTEST / 吉亮`、`cs / 销售部`。
- 使用真实代码完成临时Odoo客户映射验证、报价检查、原生确认、唯一任务排队及请求准备，冻结请求正确包含上述业务员和部门，无手工替换或解析模拟。
- 查回上一轮已创建的 `PH-T-260923-LT0311-01`，通过新增的业务员/部门及原金额/明细核对。
- 本次未向ERP新增订单；临时本地客户、订单、任务全部事务回滚。自动传单开关保持原来的关闭状态。

本补充是接口及原生模型流程验证，不宣称重新跑了整站人工点击UAT。

## 8. 客户业务员分配补充验收（19.0.1.2.0）

已刷新C4凭证，实际确认员工、部门、业务单元三个详情查询接口可用。新增能力沿用原生品牌、客户及注册审核表单，没有建立第二套员工档案，也不要求ERP业务员拥有Odoo账号。

### 自动化回归

全部用友模块 **76项测试，0失败、0错误**；日志 `output/yonyou-salesperson-assignment-final-tests.log`，结束时间2026-09-23 09:21:16 UTC。覆盖人员/部门有效性、组织隔离、多部门歧义、品牌默认与申请覆盖、已有客户只读、防伪造验证结果、失败清除旧验证、创建请求冻结及负责人查回不一致时保持待审等场景。

### 8072人工点击验收

| 操作 | 实际结果 |
| --- | --- |
| 打开注册申请 REG/2026/00134，验证品牌预填的 LQS0008 / 05 | 该人员不属于目标测试组织999，明确拒绝，未创建ERP客户 |
| 改填 LQSTEST，清空部门，点击 Verify Salesperson | 自动识别唯一部门 cs，显示吉亮 / 销售部及 Verified |
| 点击客户分类旁 Verify | 分类05验证成功，已选择的业务员和部门保持不变 |
| 点击 Approve & Activate | 实际创建测试ERP客户，查回一致后完成审批，状态 Access Activated |
| 点击新公司链接进入 Partner Hub，再点 Verify & Refresh | 展示正确人员姓名、部门及编码，绑定后人员字段只读 |
| 打开 UAT Existing LT2026030311 的 Partner Hub，点 Verify & Refresh | 从真实ERP客户带出 LQSTEST / 吉亮、cs / 销售部、客户分类06；人员字段变为只读，没有修改ERP客户 |

本次保留的测试数据：Odoo申请134，公司936（Partner Hub Salesperson UAT 20260923）；ERP测试客户编码 `PH_TEST_1962D7C4CF5CA78E`，ID `2636727772115894428`。管理组织和使用组织均为999，没有向正式组织创建客户。Odoo公司935用于已有客户只读查询验收。

### 下单参数与边界

使用新批准客户的个人联系人完成原生报价检查、订单确认、唯一任务排队与请求准备，核对请求客户编码及 `corpContact=LQSTEST`、`saleDepartmentId=cs`。这部分为真实模型及真实ERP查询测试，不是人工支付页面测试；临时报价、任务和开关变更全部事务回滚，本轮未再向ERP新增销售订单。

8072已升级并保持运行，登录页HTTP 200。注册同步配置保留；自动传单开关仍保持关闭，没有因升级擅自启用。操作说明第3节已根据本次实际表单和点击流程重写。本轮未推送GitHub或部署Odoo.sh。
